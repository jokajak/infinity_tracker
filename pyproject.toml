[tool.poetry]
name = "infinity_tracker"
version = "0.1.0"
description = ""
authors = ["jokajak <jokajak@gmail.com>"]

[tool.poetry.dependencies]
python = ">=3.6.1"
Django = "^3.0.8"

[tool.poetry.dev-dependencies]
pre-commit = "^2.6.0"
black = "^19.10b0"
flake8 = "^3.8.3"

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
